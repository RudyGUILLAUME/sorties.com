<nav class="navbar navbar-expand-lg navbar-dark bg-dark bg-gradient shadow-sm">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Logo Sorties.com" height="50" class="me-2">
            <span class="fw-bold text-white">Sorties.com</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link active" href="{{ path('app_home') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_sortie_index') }}">Voir les sorties</a></li>
                {% if app.user %}
                    {% if is_granted("ROLE_ADMIN") %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_lieu_index') }}">Lieux</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_ville_index') }}">Villes</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_panel') }}">Panel Admin</a>
                        </li>
                    {% endif %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile_show', {'id': app.user.id}) }}">
                            {{ app.user.prenom }}
                            {% if 'ROLE_ADMIN' in app.user.roles %}
                                <small class="text-warning">(Admin)</small>
                            {% elseif 'ROLE_ORGANISATEUR' in app.user.roles %}
                                <small class="text-info">(Organisateur)</small>
                            {% endif %}
                        </a>
                    </li>

                    <li>
                        <a href="{{ path('app_logout') }}" class="Btn-logout">
                            <div class="sign-logout">
                                <svg viewBox="0 0 512 512">
                                    <path d="M377.9 105.9L500.7 228.7c7.2 7.2 11.3 17.1 11.3 27.3s-4.1 20.1-11.3 27.3L377.9 406.1c-6.4 6.4-15 9.9-24 9.9c-18.7 0-33.9-15.2-33.9-33.9l0-62.1-128 0c-17.7 0-32-14.3-32-32l0-64c0-17.7 14.3-32 32-32l128 0 0-62.1c0-18.7 15.2-33.9 33.9-33.9c9 0 17.6 3.6 24 9.9zM160 96L96 96c-17.7 0-32 14.3-32 32l0 256c0 17.7 14.3 32 32 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32l-64 0c-53 0-96-43-96-96L0 128C0 75 43 32 96 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32z"></path>
                                </svg>
                            </div>
                            <div class="text-logout">Se déconnecter</div>
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#loginModal">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
