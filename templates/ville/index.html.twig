{% extends 'base.html.twig' %}

{% block title %}Villes{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h1 class="h4 mb-0">Villes</h1>
        <a class="btn btn-primary" href="{{ path('app_ville_new') }}">Nouvelle ville</a>
    </div>

    <div class="card shadow-sm">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Code postal</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for ville in villes %}
                        <tr>
                            <td>{{ ville.id }}</td>
                            <td>{{ ville.nom }}</td>
                            <td>{{ ville.codePostal }}</td>
                            <td class="text-end">
                                <div class="btn-group btn-group-sm">
                                    <a class="btn btn-outline-secondary" href="{{ path('app_ville_edit', {id: ville.id}) }}">Modifier</a>
                                    <a class="btn btn-primary" href="{{ path('app_ville_show', {id: ville.id}) }}">Afficher</a>
                                    <form method="post" action="{{ path('app_ville_delete', {id: ville.id}) }}" onsubmit="return confirm('Supprimer cette ville ?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.id) }}">
                                        <button class="btn btn-outline-danger" type="submit">Supprimer</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center text-muted py-4">Aucune ville.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}


