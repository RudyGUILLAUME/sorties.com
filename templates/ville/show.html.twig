{% extends 'base.html.twig' %}

{% block title %}Détail de la ville{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">{{ ville.nom }}</h1>
    <div class="card shadow-sm">
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3">ID</dt>
                <dd class="col-sm-9">{{ ville.id }}</dd>
                <dt class="col-sm-3">Nom</dt>
                <dd class="col-sm-9">{{ ville.nom }}</dd>
                <dt class="col-sm-3">Code postal</dt>
                <dd class="col-sm-9">{{ ville.codePostal }}</dd>
            </dl>
            <div class="mt-3 d-flex gap-2">
                <a href="{{ path('app_ville_index') }}" class="btn btn-secondary">Retour</a>
                <a href="{{ path('app_ville_edit', {id: ville.id}) }}" class="btn btn-primary">Modifier</a>
                <form method="post" action="{{ path('app_ville_delete', {id: ville.id}) }}" onsubmit="return confirm('Supprimer cette ville ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.id) }}">
                    <button class="btn btn-outline-danger" type="submit">Supprimer</button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}


