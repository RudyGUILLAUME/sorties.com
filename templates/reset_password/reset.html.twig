{% extends 'base.html.twig' %}

{% block title %}Réinitialiser votre mot de passe{% endblock %}

{% block body %}
    <main class="flex-1 p-8 overflow-y-auto">
        <div class="max-w-md mx-auto">

            <!-- 🧭 Fil d’Ariane -->
            <div class="flex flex-wrap gap-2 mb-6 text-sm">
                <a href="{{ path('app_home') }}" class="text-primary hover:underline">Accueil</a>
                <span class="text-subtext-light dark:text-subtext-dark">/</span>
                <span class="text-text-light dark:text-text-dark">Réinitialiser le mot de passe</span>
            </div>

            <!-- 🔐 En-tête -->
            <div class="text-center mb-10">
                <h1 class="text-3xl font-extrabold text-text-light dark:text-text-dark">Réinitialiser votre mot de passe</h1>
                <p class="text-subtext-light dark:text-subtext-dark mt-2 text-sm">
                    Entrez votre nouveau mot de passe ci-dessous pour sécuriser votre compte.
                </p>
            </div>

            <!-- 🧾 Formulaire -->
            <div class="bg-white dark:bg-[#192633] rounded-xl border border-border-light dark:border-border-dark shadow-sm p-8">
                {{ form_start(resetForm) }}

                <!-- Champ mot de passe -->
                <div class="mb-6">
                    <label for="{{ resetForm.plainPassword.first.vars.id }}"
                           class="block text-sm font-medium text-text-light dark:text-text-dark mb-1">
                        Nouveau mot de passe
                    </label>
                    {{ form_widget(resetForm.plainPassword.first, {
                        'attr': {
                            'type': 'password',
                            'class': 'w-full h-11 rounded-lg border border-border-light dark:border-border-dark
                                  bg-background-light dark:bg-background-dark text-text-light dark:text-text-dark
                                  px-4 text-sm focus:ring-2 focus:ring-primary outline-none transition',
                            'placeholder': '••••••••'
                        }
                    }) }}
                    {{ form_errors(resetForm.plainPassword.first) }}

                    <label for="{{ resetForm.plainPassword.second.vars.id }}"
                           class="block text-sm font-medium text-text-light dark:text-text-dark mt-4 mb-1">
                        Confirmer le mot de passe
                    </label>
                    {{ form_widget(resetForm.plainPassword.second, {
                        'attr': {
                            'type': 'password',
                            'class': 'w-full h-11 rounded-lg border border-border-light dark:border-border-dark
                                  bg-background-light dark:bg-background-dark text-text-light dark:text-text-dark
                                  px-4 text-sm focus:ring-2 focus:ring-primary outline-none transition',
                            'placeholder': '••••••••'
                        }
                    }) }}
                    {{ form_errors(resetForm.plainPassword.second) }}
                </div>

                <!-- Bouton -->
                <div class="flex justify-end">
                    <button type="submit"
                            class="h-11 px-6 flex items-center justify-center rounded-lg bg-primary text-white text-sm font-semibold hover:bg-primary/90 transition">
                        Réinitialiser le mot de passe
                    </button>
                </div>

                {{ form_end(resetForm) }}
            </div>

            <!-- 🔙 Retour -->
            <div class="text-center mt-6">
                <a href="{{ path('app_login') }}"
                   class="text-primary hover:underline text-sm font-medium">
                    ← Retour à la connexion
                </a>
            </div>
        </div>
    </main>
{% endblock %}
