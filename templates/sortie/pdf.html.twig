<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
{% block body %}
    <div class="container py-4" style="font-family: 'Segoe UI', Arial, sans-serif;">
        <!-- HEADER -->
        <div class="text-center mb-4 border-bottom pb-3">
            <h1 class="fw-bold text-primary mb-1">{{ sortie.nom }}</h1>
            <h5 class="text-secondary">Organisé par <strong>{{ sortie.site.nom }}</strong></h5>
        </div>

        <!-- IMAGE -->
        {% if sortie.imagePrincipale %}
            <div class="text-center mb-4">
                <img src="{{ absolute_url(asset('images/sorties/' ~ sortie.imagePrincipale)) }}"
                     alt="Image de {{ sortie.nom }}"
                     class="img-fluid rounded border border-primary"
                     style="max-height: 300px;">
            </div>
        {% endif %}

        <!-- INFORMATIONS -->
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-primary text-white fw-bold">
                Informations sur la sortie
            </div>
            <div class="card-body">
                <div class="row mb-2">
                    <div class="col-5 fw-semibold text-secondary">Date de début :</div>
                    <div class="col-7">{{ sortie.dateHeureDebut|date('d/m/Y H:i') }}</div>
                </div>
                <div class="row mb-2">
                    <div class="col-5 fw-semibold text-secondary">Clôture inscriptions :</div>
                    <div class="col-7">{{ sortie.dateLimiteInscription|date('d/m/Y H:i') }}</div>
                </div>
                <div class="row mb-2">
                    <div class="col-5 fw-semibold text-secondary">Durée :</div>
                    <div class="col-7">{{ sortie.duree }} minutes</div>
                </div>
                <div class="row mb-2">
                    <div class="col-5 fw-semibold text-secondary">Places disponibles :</div>
                    <div class="col-7">{{ sortie.nbInscriptionsMax }}</div>
                </div>
                <div class="row mb-2">
                    <div class="col-5 fw-semibold text-secondary">Lieu :</div>
                    <div class="col-7">{{ sortie.lieu.nom }}</div>
                </div>
                <div class="row mb-2">
                    <div class="col-5 fw-semibold text-secondary">Site :</div>
                    <div class="col-7">{{ sortie.site.nom }}</div>
                </div>
                <div class="row">
                    <div class="col-5 fw-semibold text-secondary">Informations :</div>
                    <div class="col-7">{{ sortie.infosSortie }}</div>
                </div>
            </div>
        </div>

        <!-- PARTICIPANTS -->
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-success text-white fw-bold">
                Participants ({{ sortie.participants|length }})
            </div>
            <div class="card-body">
                {% if sortie.participants is not empty %}
                    <ul class="list-group list-group-flush">
                        {% for participant in sortie.participants %}
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span>{{ participant.prenom }} {{ participant.nom }}</span>
                                {% if participant.imageProfil %}
                                    <img src="{{ absolute_url(asset('images/uploads/' ~ participant.imageProfil)) }}"
                                         alt="Photo de {{ participant.prenom }}"
                                         class="rounded-circle border"
                                         width="40" height="40">
                                {% endif %}
                            </li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <p class="text-muted fst-italic">Aucun participant pour le moment.</p>
                {% endif %}
            </div>
        </div>

        <!-- PIED DE PAGE -->
        <div class="text-center mt-5 pt-3 border-top text-muted small">
            <p>Affiche générée automatiquement - {{ "now"|date("d/m/Y H:i") }}</p>
            <p>© École MyCampus - Département Développement</p>
        </div>
    </div>
{% endblock %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
